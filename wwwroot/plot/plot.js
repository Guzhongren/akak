define([
    'dojo/_base/declare',
    "plot/symobl"
], function (declare,
    Symobl,
    ) {
        return clazz = declare([], function () {
            console.log(Symbol.PolygonActiveSymbol);
        });
    });